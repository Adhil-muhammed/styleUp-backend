export enum ShopStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
  SUSPENDED = 'suspended',
}

export enum StaffAvailabilityStatus {
  AVAILABLE = 'available',
  BUSY = 'busy',
  OFF = 'off',
}

export enum StaffWorkflowStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  REJECTED = 'rejected',
}
